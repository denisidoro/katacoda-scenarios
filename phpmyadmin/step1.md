### Step: Getting the YML file

First clone the repo with the docker-compose.yml file:
```
git clone https://github.com/jcavat/docker-lamp
cd docker-lamp
```{{execute T1}}

### Step: Start

Then, start the containers:
```
docker-compose up -d
```{{execute T1}}